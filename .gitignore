# Obsidian local workspace state (machine-specific, not worth versioning)
**/.obsidian/workspace.json
**/.obsidian/workspace-mobile.json
**/.trash/

# Obsidian plugin code (reinstalled from community-plugins.json, not worth versioning)
**/.obsidian/plugins/

# Stray Obsidian config at the repo root - the root is a container for the
# vaults below it, not a vault itself
/.obsidian/

# macOS
.DS_Store
**/.DS_Store

# macOS zip-extraction resource forks (junk, no content)
__MACOSX/
